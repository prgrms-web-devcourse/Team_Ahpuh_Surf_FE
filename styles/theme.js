const fontColor = {
  $font: '#373530',
}

const surfColor = {
  $blue__1: '#d0ebff',
  $blue__2: '#74c0fc',
  $blue__3: '#339af0',
  $blue__4: '#1c73d6',
  $blue__5: '#1864ab',
}

const grayColor = {
  $border: 'rgba(0, 0, 0, 0.3)',
}

const commonSize = {
  $Header: 80,
  $Navbar: 50,
}
const theme = {
  fontColor,
  surfColor,
  grayColor,
  commonSize,
}

export default theme
